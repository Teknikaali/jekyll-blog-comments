using System;

namespace ApplicationCore
{
    [Obsolete("Use some other mechanism to deal with missing required values", error: false)]
    public struct MissingRequiredValue { } // Placeholder for missing required form values
}
